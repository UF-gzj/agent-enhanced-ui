# Knowledge Index

1. Use `/prim`, `/pln`, `/exec`, `/revu`, `/vald`, `/iter`, `/rca`, `/fix`, `/cmit` to cover main and bugfix chains.
2. Keep `.claude/docs` populated with one artifact per major kind so artifact binding can be exercised.
3. Treat this workspace as disposable test input, not long-term product knowledge.
